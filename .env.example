## Privy relay server (server-side only)
# DO NOT put secrets in Vite `VITE_*` env vars.
# Copy to `server/.env` (or set in your hosting environment).

PRIVY_APP_ID=cmmv0z6dv06bs0djs07c7vrl3
PRIVY_APP_SECRET=replace_me

# Relay server port
PORT=8787

